package freemind.diagram.mindmap;

import freemind.diagram.DiagramNode;

/**
 * Node in a {@link MindMapDiagram}. Currently a marker — may grow mind-map-specific
 * accessors (fold state, side preference) as Plan 2 wires up the running app.
 */
public interface MindMapNode extends DiagramNode {
}
