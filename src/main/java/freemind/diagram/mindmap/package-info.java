/**
 * Concrete MindMap implementation of the {@link freemind.diagram.Diagram}
 * abstraction. Provides {@link MindMapDiagram}, {@link MindMapNode},
 * an in-memory model, the native payload codec, and a one-way importer for
 * the legacy {@code .mm} format.
 *
 * <p>NOT to be confused with {@code freemind.model.MindMap} /
 * {@code freemind.model.MindMapNode}, which are the legacy types still in
 * use by the running application. Those are migrated in Plan 2.
 */
package freemind.diagram.mindmap;
