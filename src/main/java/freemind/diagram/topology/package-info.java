/**
 * Topology-specific {@link freemind.diagram.Diagram} subtypes.
 * {@link TreeDiagram} for parent/child structures (mind maps, fishbones);
 * {@link GraphDiagram} for arbitrary labeled directed graphs (concept maps).
 *
 * <p>A diagram may implement both (a tree-shaped graph) or compose with capability
 * mixins from {@code freemind.diagram.capabilities} for cross-cutting features.
 */
package freemind.diagram.topology;
